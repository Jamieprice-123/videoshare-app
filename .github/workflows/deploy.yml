name: Deploy VideoShare App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy-api:
    runs-on: ubuntu-latest
    name: Build and Deploy API
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          cd api
          npm install

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Functions
        run: |
          cd api
          npm install -g azure-functions-core-tools@4 --unsafe-perm true
          func azure functionapp publish videoshare-api-jp

  deploy-frontend:
    runs-on: ubuntu-latest
    name: Deploy Frontend
    needs: build-and-deploy-api
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Upload to Blob Storage
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az storage blob upload-batch \
              --account-name videosharestorejp \
              --destination '$web' \
              --source ./frontend \
              --overwrite